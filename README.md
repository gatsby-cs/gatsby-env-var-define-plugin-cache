# gatsby-env-var-define-plugin-cache

Reproduction of issue where env vars change between builds but the values aren't changed during function invocation.

Not able to reproduce locally, will test in cloud.
